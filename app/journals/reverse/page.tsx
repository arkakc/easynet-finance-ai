"use client";

import { FormEvent, useState } from "react";
export default function JournalReversalPage(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);try{const r=await fetch("/api/erp/actions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({target:"journalReverse",body:{payload:Object.fromEntries(form.entries())}})});const b=await r.json();if(!r.ok||!b.ok)throw new Error(b.error||"Reversal failed");setMessage(JSON.stringify(b,null,2));e.currentTarget.reset()}catch(x){setMessage(x instanceof Error?x.message:"Reversal failed")}}return <><h2>Journal Reversal</h2><p className="small">Posted journals remain immutable. Finance approval permission is required.</p><section className="panel warning-panel"><strong>Control:</strong> The reversal remains permanently linked to the original journal.</section>{message&&<pre className="panel status-pre">{message}</pre>}<form className="panel form-grid" onSubmit={submit}><h3 className="form-title">Reverse Posted Journal</h3><label>Original Journal ID<input name="journalId" required placeholder="JRN-..."/></label><label>Reversal Date<input name="reversalDate" type="date" required/></label><label className="form-wide">Reason<textarea name="reason" rows={3} required/></label><div className="form-wide"><button type="submit">Create Reversal Journal</button></div></form></>}
