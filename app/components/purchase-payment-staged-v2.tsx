"use client";

import Link from "next/link";
import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { FlowCreateLink, useFlowDataRefresh } from "@/app/components/flow-navigation";
import AccountPicker from "@/app/components/account-picker";

type Mode="menu"|"invoice"|"direct";type InvoiceStage="browse"|"form";type Master={suppliers:any[];projects:any[]};type CashBankAccount={accountId:string;accountCode:string;accountName:string;balance:number};
const money=(value:unknown)=>`K${Number(value||0).toFixed(2)}`;const normalize=(value:unknown)=>String(value||"").trim().toUpperCase().replace(/[^A-Z0-9]/g,"");const paymentEligibleStatus=(value:unknown)=>["POSTED","PARTLY_PAID"].includes(String(value||"").toUpperCase());const returnQuery="returnModule=purchase&returnTab=purchasePayment&returnMode=create";
function localDate(){const parts=new Intl.DateTimeFormat("en-US",{timeZone:"Pacific/Port_Moresby",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(new Date());const v=Object.fromEntries(parts.map(p=>[p.type,p.value]));return`${v.year}-${v.month}-${v.day}`;}function supplierId(row:any){return String(row.supplierId||"");}function supplierName(row:any){return String(row.supplierName||supplierId(row));}function supplierDisplay(row:any){const id=supplierId(row),name=supplierName(row);return id&&name!==id?`${name} (${id})`:name;}function resolveSupplier(rows:any[],input:string){const q=input.trim().toLowerCase();if(!q)return null;return rows.find(row=>supplierDisplay(row).toLowerCase()===q)||rows.find(row=>supplierId(row).toLowerCase()===q)||rows.find(row=>supplierName(row).toLowerCase()===q)||null;}function projectDisplay(row:any){const id=String(row.projectId||"");const name=String(row.projectName||id);return id&&name!==id?`${name} (${id})`:name||"—";}

export default function PurchasePaymentStagedV2(){
  const router=useRouter();const[mode,setMode]=useState<Mode>("menu");const[invoiceStage,setInvoiceStage]=useState<InvoiceStage>("browse");const[status,setStatus]=useState("");const[loading,setLoading]=useState(false);const[busy,setBusy]=useState(false);const[nextNo,setNextNo]=useState("AUTO");const[supplierInvoices,setSupplierInvoices]=useState<any[]>([]);const[search,setSearch]=useState("");const[searched,setSearched]=useState<any|null>(null);const[selectedInvoice,setSelectedInvoice]=useState<any|null>(null);const[masters,setMasters]=useState<Master>({suppliers:[],projects:[]});const[accounts,setAccounts]=useState<CashBankAccount[]>([]);const[supplierInput,setSupplierInput]=useState("");const[selectedSupplier,setSelectedSupplier]=useState("");const[cashBankAccountId,setCashBankAccountId]=useState("");const[baseCurrency,setBaseCurrency]=useState("PGK");
  const approved=useMemo(()=>supplierInvoices.filter(row=>paymentEligibleStatus(row.status)&&Number(row.outstandingAmount??row.totalAmount??0)>0),[supplierInvoices]);const projectOptions=useMemo(()=>masters.projects,[masters.projects]);
  function supplierLabel(id:unknown){const key=String(id||"");const row=masters.suppliers.find(item=>supplierId(item)===key);return row?supplierDisplay(row):key||"—";}function projectLabel(id:unknown){const key=String(id||"");if(!key)return"No project";const row=masters.projects.find(item=>String(item.projectId||"")===key);return row?projectDisplay(row):key;}
  const refreshMasters=useCallback(async()=>{try{const response=await fetch("/api/masters",{cache:"no-store"});const body=await response.json();if(response.ok&&body.ok)setMasters({suppliers:body.suppliers||[],projects:body.projects||[]});}catch{}},[]);useFlowDataRefresh(()=>{void refreshMasters();});
  async function loadNextNo(){const response=await fetch("/api/erp/transactions?nextNumberFor=createPayment&partyType=Supplier",{cache:"no-store"});const body=await response.json();setNextNo(response.ok&&body.ok?body.nextNumber||"AUTO":"AUTO");}
  async function loadWorkspace(){setLoading(true);try{const[txResponse,masterResponse,refResponse]=await Promise.all([fetch("/api/erp/transactions",{cache:"no-store"}),fetch("/api/masters",{cache:"no-store"}),fetch("/api/erp/reference-options",{cache:"no-store"})]);const[txBody,masterBody,refBody]=await Promise.all([txResponse.json(),masterResponse.json(),refResponse.json()]);if(!txResponse.ok||!txBody.ok)throw new Error(txBody.error||"Supplier Invoice load failed");if(!masterResponse.ok||!masterBody.ok)throw new Error(masterBody.error||"Supplier master load failed");if(!refResponse.ok||!refBody.ok)throw new Error(refBody.error||"Cash / Bank account load failed");const rows=Array.isArray(txBody.supplierBills)?txBody.supplierBills:[];setSupplierInvoices(rows);setMasters({suppliers:masterBody.suppliers||[],projects:masterBody.projects||[]});setAccounts(refBody.cashBankAccounts||[]);setBaseCurrency(String(refBody.baseCurrency||"PGK").toUpperCase());await loadNextNo();return rows;}catch(error){setStatus(error instanceof Error?error.message:"Purchase Payment workspace load failed");return[];}finally{setLoading(false);}}
  async function openInvoiceMode(){setMode("invoice");setInvoiceStage("browse");setStatus("");setSelectedInvoice(null);setSearched(null);await loadWorkspace();}async function openDirectMode(){setMode("direct");setStatus("");setSupplierInput("");setSelectedSupplier("");await loadWorkspace();}function backToPaymentChoices(){setMode("menu");setInvoiceStage("browse");setStatus("");setSearch("");setSearched(null);setSelectedInvoice(null);setSupplierInput("");setSelectedSupplier("");setCashBankAccountId("");if(typeof window!=="undefined"){const url=new URL(window.location.href);url.searchParams.delete("sourceBill");window.history.replaceState(window.history.state,"",`${url.pathname}?${url.searchParams.toString()}`);}}function selectInvoice(row:any){setSelectedInvoice(row);setSearched(row);setStatus("");setInvoiceStage("form");}
  useEffect(()=>{if(typeof window==="undefined")return;const sourceBill=new URLSearchParams(window.location.search).get("sourceBill")?.trim()||"";if(!sourceBill)return;let active=true;void(async()=>{setMode("invoice");setInvoiceStage("browse");setStatus("Loading posted Supplier Invoice…");const rows=await loadWorkspace();if(!active)return;const match=rows.find((row:any)=>String(row.billId||"")===sourceBill&&paymentEligibleStatus(row.status)&&Number(row.outstandingAmount??row.totalAmount??0)>0);if(!match){setStatus("Selected Supplier Invoice is not posted or has no outstanding amount.");return;}selectInvoice(match);})();return()=>{active=false;};},[]);
  async function searchInvoice(){const q=normalize(search);if(!q){setSearched(null);setStatus("Enter a Supplier Invoice number");return;}const match=approved.find(row=>normalize(row.billNumber)===q||normalize(row.billId)===q)||approved.find(row=>normalize(row.billNumber).includes(q)||normalize(row.billId).includes(q));if(!match){setSearched(null);setStatus(`Posted Supplier Invoice with outstanding amount not found: ${search}`);return;}setStatus("");setSearched(match);}
  async function createPayment(payload:Record<string,unknown>){const response=await fetch("/api/erp/transactions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"createPayment",payload})});const body=await response.json();if(!response.ok||!body.ok)throw new Error(body.error||"Purchase Payment draft save failed");return body.result;}
  async function saveInvoicePayment(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!selectedInvoice||busy)return;setBusy(true);setStatus("Saving Payment Entry…");try{const form=new FormData(event.currentTarget);const amount=Number(form.get("amount")||0);const outstanding=Number(selectedInvoice.outstandingAmount??selectedInvoice.totalAmount??0);if(!(amount>0))throw new Error("Payment amount must be greater than zero");if(amount>outstanding+0.001)throw new Error("Payment amount cannot exceed Supplier Invoice outstanding amount");const result=await createPayment({paymentNumber:"",paymentType:"PAY",partyType:"Supplier",partyId:selectedInvoice.supplierId,projectId:selectedInvoice.projectId||"",paymentDate:form.get("paymentDate"),amount,currency:String(selectedInvoice.currency||baseCurrency).toUpperCase(),exchangeRate:Number(form.get("exchangeRate")||0)||undefined,paymentMethod:form.get("paymentMethod"),cashBankAccountId:form.get("cashBankAccountId"),reference:form.get("reference")||"",againstDocumentType:"Supplier Invoice",againstDocumentId:selectedInvoice.billId});router.push(`/transactions/payment/${result.recordId}?${returnQuery}`);router.refresh();}catch(error){setStatus(error instanceof Error?error.message:"Purchase Payment draft save failed");}finally{setBusy(false);}}
  async function saveDirectPayment(event:FormEvent<HTMLFormElement>){event.preventDefault();if(busy)return;setBusy(true);setStatus("Saving Supplier Advance…");try{const form=new FormData(event.currentTarget);const match=selectedSupplier?masters.suppliers.find(row=>supplierId(row)===selectedSupplier):resolveSupplier(masters.suppliers,supplierInput);if(!match)throw new Error("Select a valid Supplier from the suggestions");const amount=Number(form.get("amount")||0);if(!(amount>0))throw new Error("Payment amount must be greater than zero");const currency=String(match.currency||baseCurrency).toUpperCase();const result=await createPayment({paymentNumber:"",paymentType:"PAY",partyType:"Supplier",partyId:supplierId(match),projectId:form.get("projectId")||"",paymentDate:form.get("paymentDate"),amount,currency,exchangeRate:Number(form.get("exchangeRate")||0)||undefined,paymentMethod:form.get("paymentMethod"),cashBankAccountId:form.get("cashBankAccountId"),reference:form.get("reference")||"",againstDocumentType:"",againstDocumentId:""});router.push(`/transactions/payment/${result.recordId}?${returnQuery}`);router.refresh();}catch(error){setStatus(error instanceof Error?error.message:"Supplier Advance save failed");}finally{setBusy(false);}}
  const cashBankOptions=<><option value="">Select Cash / Bank account</option>{accounts.map(account=><option key={account.accountId} value={account.accountId}>{account.accountCode || account.accountId} — {account.accountName || account.accountId} · Balance {money(account.balance)}</option>)}</>;

  return <>{status&&<section className="panel status-banner">{status}</section>}{mode==="menu"&&<section className="panel"><div className="form-title-row"><div><h3>Create New Purchase Payment Entry / Receipt</h3><p className="small">Choose a payment against a posted Supplier Invoice, or record a Supplier Advance before an invoice.</p></div></div><div className="button-row" style={{marginTop:18,alignItems:"stretch"}}><button type="button" disabled={loading} style={{minHeight:86,flex:"1 1 320px",textAlign:"left"}} onClick={()=>void openInvoiceMode()}><strong style={{display:"block"}}>Approved Supplier Invoices Pending Payment</strong><span style={{display:"block",marginTop:7,fontWeight:400}}>Search or select a posted/part-paid Supplier Invoice with outstanding balance.</span></button><button type="button" disabled={loading} className="secondary" style={{minHeight:86,flex:"1 1 320px",textAlign:"left"}} onClick={()=>void openDirectMode()}><strong style={{display:"block"}}>Direct Payment Entry Without Supplier Invoice</strong><span style={{display:"block",marginTop:7,fontWeight:400}}>Records a Supplier Advance asset until allocated.</span></button></div></section>}
    {mode!=="menu"&&<section className="panel"><div className="button-row" style={{justifyContent:"space-between"}}><button type="button" className="secondary" disabled={busy} onClick={backToPaymentChoices}>← Back to Create Purchase Payment Entry / Receipt</button><span className="auto-badge">Next Payment No: {nextNo||"AUTO"}</span></div></section>}
    {mode==="invoice"&&invoiceStage==="browse"&&<><section className="panel"><h3>Search Posted Supplier Invoice</h3><p className="small">Search comes first so a known Supplier Invoice can be opened directly for payment.</p><div className="form-grid" style={{marginTop:16}}><label>Supplier Invoice No<input value={search} onChange={event=>setSearch(event.target.value)} onKeyDown={event=>{if(event.key==="Enter"){event.preventDefault();void searchInvoice();}}} placeholder="e.g. PB-2026-00001" autoComplete="off"/></label><div style={{display:"flex",alignItems:"end"}}><button type="button" style={{width:"100%",height:52}} onClick={()=>void searchInvoice()} disabled={loading}>{loading?"Searching…":"Search"}</button></div></div>{searched&&<div style={{marginTop:20}}><div className="document-meta"><div><span>Supplier Invoice</span><strong>{searched.billNumber||searched.billId}</strong></div><div><span>Supplier</span><strong>{supplierLabel(searched.supplierId)}</strong></div><div><span>Project</span><strong>{projectLabel(searched.projectId)}</strong></div><div><span>Invoice Total</span><strong>{money(searched.totalAmount)}</strong></div><div><span>Outstanding</span><strong>{money(searched.outstandingAmount??searched.totalAmount)}</strong></div><div><span>Status</span><strong>{searched.status}</strong></div></div><div className="button-row" style={{marginTop:18}}><button type="button" onClick={()=>selectInvoice(searched)}>Create Payment Entry</button></div></div>}</section><section className="panel table-wrap"><div className="form-title-row"><h3>Approved Supplier Invoices Pending Payment</h3><span className="auto-badge">{loading?"Loading…":`${approved.length} Pending`}</span></div><table className="data-table"><thead><tr><th>Supplier Invoice</th><th>Supplier</th><th>Project</th><th>Invoice Total</th><th>Outstanding</th><th>Action</th></tr></thead><tbody>{!loading&&approved.length===0&&<tr><td colSpan={6}>No posted Supplier Invoices with outstanding balance.</td></tr>}{approved.map(invoice=><tr key={invoice.billId}><td><Link href={`/transactions/supplierBill/${invoice.billId}?${returnQuery}`}><strong>{invoice.billNumber||invoice.billId}</strong></Link></td><td>{supplierLabel(invoice.supplierId)}</td><td>{projectLabel(invoice.projectId)}</td><td>{money(invoice.totalAmount)}</td><td><strong>{money(invoice.outstandingAmount??invoice.totalAmount)}</strong></td><td><button type="button" onClick={()=>selectInvoice(invoice)}>Create Payment</button></td></tr>)}</tbody></table></section></>}
    {mode==="invoice"&&invoiceStage==="form"&&selectedInvoice&&<><section className="panel"><div className="button-row"><button type="button" className="secondary" disabled={busy} onClick={()=>{setInvoiceStage("browse");setSelectedInvoice(null);}}>← Back to Supplier Invoice Search / List</button></div></section><form className="panel form-grid" onSubmit={saveInvoicePayment}><h3 className="form-title">Purchase Payment Entry Against Supplier Invoice</h3><label>Supplier Invoice<input value={selectedInvoice.billNumber||selectedInvoice.billId} readOnly/></label><label>Supplier<input value={supplierLabel(selectedInvoice.supplierId)} readOnly/></label><label>Project<input value={projectLabel(selectedInvoice.projectId)} readOnly/></label><label>Auto Payment No<input value={nextNo||"AUTO"} readOnly/></label><label>Invoice Total<input value={money(selectedInvoice.totalAmount)} readOnly/></label><label>Outstanding<input value={money(selectedInvoice.outstandingAmount??selectedInvoice.totalAmount)} readOnly/></label><label>Payment Date<input name="paymentDate" type="date" defaultValue={localDate()} required disabled={busy}/></label><label>Currency<input value={String(selectedInvoice.currency||baseCurrency).toUpperCase()} readOnly/></label>{String(selectedInvoice.currency||baseCurrency).toUpperCase()!==baseCurrency&&<label>Settlement Exchange Rate<input name="exchangeRate" type="number" min="0.00000001" step="0.00000001" placeholder={`1 ${String(selectedInvoice.currency||"")} = ? ${baseCurrency}`} disabled={busy}/><span className="small">Leave blank to use the latest approved rate on/before payment date.</span></label>}<label>Amount<input name="amount" type="number" min="0.01" max={Number(selectedInvoice.outstandingAmount??selectedInvoice.totalAmount??0)} step="0.01" defaultValue={Number(selectedInvoice.outstandingAmount??selectedInvoice.totalAmount??0)} required disabled={busy}/></label><label>Payment Method<select name="paymentMethod" defaultValue="" required disabled={busy}><option value="">Select payment method</option><option>Cash</option><option>Bank Transfer</option><option>Card</option><option>Cheque</option></select></label><label>Cash / Bank Account<AccountPicker name="cashBankAccountId" value={cashBankAccountId} onChange={setCashBankAccountId} accounts={accounts} kind="cash-bank" required disabled={busy} placeholder="Search cash/bank account by name or code"/><span className="small">Account Name, ID and current GL balance are shown. Final Save will block an overdraft.</span></label><label className="form-wide">Reference<input name="reference" placeholder="Bank / payment reference" disabled={busy}/></label><div className="form-wide button-row"><button type="submit" disabled={busy} style={busy ? { opacity: 0.6, cursor: "not-allowed", filter: "grayscale(1)" } : undefined}>{busy?"Saving Draft…":"Save Payment Draft"}</button></div></form></>}
    {mode==="direct"&&<form className="panel form-grid" onSubmit={saveDirectPayment}><div className="form-wide form-title-row"><div><h3 className="form-title">Direct Supplier Advance Without Supplier Invoice</h3><p className="small">If the Supplier or Project is missing, create it in another tab. This advance form stays open and refreshes its master choices when you return.</p></div><div className="button-row"><FlowCreateLink target="supplier" label="Create New Supplier in New Tab" returnLabel="Supplier Advance"/><FlowCreateLink target="project" label="Create New Project in New Tab" returnLabel="Supplier Advance"/></div></div><label>Supplier<input list="direct-payment-supplier-suggestions" value={supplierInput} onChange={event=>{const value=event.target.value;setSupplierInput(value);const match=resolveSupplier(masters.suppliers,value);setSelectedSupplier(match?supplierId(match):"");}} placeholder="Type supplier name or ID" autoComplete="off" required disabled={busy}/><datalist id="direct-payment-supplier-suggestions">{masters.suppliers.map(row=><option key={supplierId(row)} value={supplierDisplay(row)}/>)}</datalist></label><label>Project<select name="projectId" defaultValue="" disabled={busy}><option value="">No project</option>{projectOptions.map(row=><option key={row.projectId} value={row.projectId}>{projectDisplay(row)}</option>)}</select></label><label>Auto Payment No<input value={nextNo||"AUTO"} readOnly/></label><label>Payment Date<input name="paymentDate" type="date" defaultValue={localDate()} required disabled={busy}/></label>{(()=>{const directSupplier=selectedSupplier?masters.suppliers.find(row=>supplierId(row)===selectedSupplier):resolveSupplier(masters.suppliers,supplierInput);const currency=String(directSupplier?.currency||baseCurrency).toUpperCase();return <><label>Currency<input value={currency} readOnly/></label>{currency!==baseCurrency&&<label>Settlement Exchange Rate<input name="exchangeRate" type="number" min="0.00000001" step="0.00000001" placeholder={`1 ${currency} = ? ${baseCurrency}`} disabled={busy}/><span className="small">Leave blank to use the latest approved rate on/before payment date.</span></label>}</>;})()}<label>Amount<input name="amount" type="number" min="0.01" step="0.01" required disabled={busy}/></label><label>Payment Method<select name="paymentMethod" defaultValue="" required disabled={busy}><option value="">Select payment method</option><option>Cash</option><option>Bank Transfer</option><option>Card</option><option>Cheque</option></select></label><label>Cash / Bank Account<AccountPicker name="cashBankAccountId" value={cashBankAccountId} onChange={setCashBankAccountId} accounts={accounts} kind="cash-bank" required disabled={busy} placeholder="Search cash/bank account by name or code"/><span className="small">Final Save cannot make a normal Cash / Bank account negative.</span></label><label className="form-wide">Reference<input name="reference" placeholder="Bank / payment reference" disabled={busy}/></label><div className="form-wide"><p className="small">Accounting on Final Save: Dr Supplier Advances (ACC-1160), Cr Cash/Bank. It does not reduce Accounts Payable until allocated to a posted Supplier Invoice.</p></div><div className="form-wide button-row"><button type="submit" disabled={busy||loading} style={(busy||loading) ? { opacity: 0.6, cursor: "not-allowed", filter: "grayscale(1)" } : undefined}>{busy?"Saving Draft…":"Save Supplier Advance Draft"}</button></div></form>}
  </>;
}
