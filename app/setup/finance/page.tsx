"use client";

import { FormEvent, useState } from "react";
export default function FinanceSetupPage(){const [result,setResult]=useState("");const [running,setRunning]=useState(false);async function runSetup(e:FormEvent<HTMLFormElement>){e.preventDefault();setRunning(true);setResult("Initializing finance master data...");try{const r=await fetch("/api/erp/actions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({target:"setupFinance",body:{}})});const b=await r.json();setResult(JSON.stringify(b,null,2))}catch(x){setResult(x instanceof Error?x.message:"Setup request failed")}finally{setRunning(false)}}return <><h2>Finance Initialization</h2><p className="small">One-time finance initialization. System Manager / Settings permission is required; no shared secret is entered in the browser.</p><form className="panel" onSubmit={runSetup}><button type="submit" disabled={running}>{running?"Initializing...":"Initialize Finance Data"}</button></form>{result&&<pre className="panel" style={{whiteSpace:"pre-wrap"}}>{result}</pre>}</>}
