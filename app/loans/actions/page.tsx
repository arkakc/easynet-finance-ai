"use client";

import { FormEvent, useState } from "react";
export default function LoanActionsPage(){const [message,setMessage]=useState("");async function call(action:"accrue"|"repay",payload:Record<string,FormDataEntryValue>){const r=await fetch("/api/erp/actions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({target:"loanActions",body:{action,payload}})});const b=await r.json();if(!r.ok||!b.ok)throw new Error(b.error||"Loan action failed");setMessage(JSON.stringify(b.result,null,2))}async function accrue(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await call("accrue",Object.fromEntries(new FormData(e.currentTarget).entries()))}catch(x){setMessage(x instanceof Error?x.message:"Accrual failed")}}async function repay(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await call("repay",Object.fromEntries(new FormData(e.currentTarget).entries()))}catch(x){setMessage(x instanceof Error?x.message:"Repayment failed")}}return <><h2>Loan Actions</h2><p className="small">Controlled by signed-in user permissions.</p>{message&&<pre className="panel status-pre">{message}</pre>}<form className="panel form-grid" onSubmit={accrue}><h3 className="form-title">Accrue Compound Monthly Interest</h3><label>Loan ID<input name="loanId" required defaultValue="LOAN-2026-0001"/></label><label>Accrue Through<input name="asOf" type="date" required/></label><div className="form-wide"><button type="submit">Post Interest Accrual</button></div></form><form className="panel form-grid" onSubmit={repay}><h3 className="form-title">Loan Repayment</h3><label>Loan ID<input name="loanId" required defaultValue="LOAN-2026-0001"/></label><label>Payment Date<input name="paymentDate" type="date" required/></label><label>Principal Amount<input name="principalAmount" type="number" min="0" step="0.01" defaultValue="0"/></label><label>Interest Amount<input name="interestAmount" type="number" min="0" step="0.01" defaultValue="0"/></label><label>Cash/Bank Account<input name="cashBankAccountId" required defaultValue="ACC-1110"/></label><label>Method<select name="paymentMethod"><option>Cash</option><option>Bank Transfer</option><option>Cheque</option></select></label><label className="form-wide">Reference<input name="reference"/></label><div className="form-wide"><button type="submit">Post Repayment</button></div></form></>}
