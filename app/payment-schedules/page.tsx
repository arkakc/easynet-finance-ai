"use client";

import { FormEvent, useEffect, useState } from "react";
export default function PaymentSchedulesPage(){const[schedules,setSchedules]=useState<any[]>([]),[quotes,setQuotes]=useState<any[]>([]),[invoices,setInvoices]=useState<any[]>([]),[projects,setProjects]=useState<any[]>([]),[message,setMessage]=useState("");async function load(){try{const[s,t,m]=await Promise.all([fetch("/api/payment-schedules").then(r=>r.json()),fetch("/api/erp/transactions").then(r=>r.json()),fetch("/api/masters").then(r=>r.json())]);if(!s.ok)throw new Error(s.error||"Schedule load failed");if(!t.ok)throw new Error(t.error||"Transaction load failed");if(!m.ok)throw new Error(m.error||"Master-data load failed");setSchedules(s.schedules||[]);setQuotes(t.quotes||[]);setInvoices(t.invoices||[]);setProjects(m.projects||[])}catch(e){setMessage(e instanceof Error?e.message:"Load failed")}}useEffect(()=>{void load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);try{const r=await fetch("/api/erp/actions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({target:"paymentSchedules",body:{record:Object.fromEntries(form.entries())}})});const b=await r.json();if(!r.ok||!b.ok)throw new Error(b.error||"Schedule save failed");setMessage(`Milestone saved. Total scheduled: ${Number(b.totalPercentage).toFixed(2)}%.`);e.currentTarget.reset();await load()}catch(x){setMessage(x instanceof Error?x.message:"Schedule save failed")}}return <><h2>Payment Schedules</h2><p className="small">Sales permission controls milestone creation.</p>{message&&<section className="panel"><strong>Status:</strong> {message}</section>}<form className="panel form-grid" onSubmit={submit}><h3 className="form-title">New Milestone</h3><label>Source Type<select name="sourceType" defaultValue="QUOTE"><option>QUOTE</option><option>INVOICE</option><option>PROJECT</option></select></label><label>Source ID<input name="sourceId" required/></label><label>Project<select name="projectId" defaultValue=""><option value="">No project</option>{projects.map(p=><option key={p.projectId} value={p.projectId}>{p.projectName} ({p.projectId})</option>)}</select></label><label>Party ID<input name="partyId"/></label><label>Milestone<input name="milestone" required/></label><label>Due Date<input name="dueDate" type="date"/></label><label>Percentage<input name="percentage" type="number" min="0" max="100" step="0.01" required/></label><label>Amount<input name="amount" type="number" min="0" step="0.01" required/></label><div className="form-wide"><button type="submit">Save Milestone</button></div></form><section className="panel table-wrap"><h3>Milestone Register</h3><table className="data-table"><thead><tr><th>Schedule</th><th>Source</th><th>Project</th><th>Party</th><th>Milestone</th><th>Due</th><th>%</th><th>Amount</th><th>Status</th></tr></thead><tbody>{schedules.map(row=><tr key={row.scheduleId}><td>{row.scheduleId}</td><td>{row.sourceType} {row.sourceId}</td><td>{row.projectId||"—"}</td><td>{row.partyId||"—"}</td><td>{row.milestone}</td><td>{row.dueDate||"—"}</td><td>{Number(row.percentage||0).toFixed(2)}%</td><td>K{Number(row.amount||0).toFixed(2)}</td><td>{row.status}</td></tr>)}{!schedules.length&&<tr><td colSpan={9}>No payment schedules found.</td></tr>}</tbody></table></section><section className="panel"><h3>Reference Documents</h3><p className="small">Sales Quotations: {quotes.length} · Sales Invoices: {invoices.length} · Projects: {projects.length}</p></section></>}
